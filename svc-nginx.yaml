apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80       # Service暴露的端口
      targetPort: 80 # 容器的端口
  type: ClusterIP    # 服务类型，可改为 NodePort / LoadBalancer
